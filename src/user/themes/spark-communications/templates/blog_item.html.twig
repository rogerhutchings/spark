{% import 'blog/macros.html.twig' as blogMacros %}
{% extends 'blog/blog_layout.html.twig' %}

{% set header_image = page.media[page.header.header_image]|default(false) %}
{% set author = page.find(page.header.author)|default(false) %}

{% block blog_content %}
  {% if header_image %}
    {{ header_image.html(null, null, 'w-100') }}
  {% endif %}

  <h1 class="lh-title orange f2 fw4">
    {{ page.title }}
  </h1>

  {{ blogMacros.blogMeta(page, true) }}

  <section class="blog">
    {{ content }}
  </section>

  <div class="js-share">
  </div>

{% endblock blog_content %}
