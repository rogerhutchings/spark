title: Why Spark?
'@extends':
    type: default
    context: blueprints://pages

form:

  fields:

    tabs:
      type: tabs
      active: 1

      fields:
        content:

          fields:
            header.quotes:
              type: list
              autofocus: true
              style: vertical
              label: Quotes
              fields:
                .body:
                  label: Quote body
                  type: text
                  help: No need to add quotation marks - they're added automatically on the page.
                .source:
                  label: Quote source
                  type: text
