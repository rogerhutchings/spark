FROM ahumaro/grav-php-nginx

WORKDIR /usr/share/nginx/html/

# Provide mount point for local repo data
RUN rm -fR user/
RUN mkdir -p user/

# Fix permissions
RUN chmod +x bin/*
RUN chown -R www-data:www-data *
